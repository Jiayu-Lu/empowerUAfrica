
const admin = {
    isAdmin: (username) => {
        // TODO: Finish this function
        if (username === 'admin') {
            return true; 
        }
        return false;
    }
}

module.exports = admin; 